(in-package :daft)

;;------------------------------------------------------------

(define-god () (:main))

;;------------------------------------------------------------

(defun+ ensure-god ()
  (declare (profile t))
  (unless *god*
    (let ((*self* nil))
      (setf *god* (spawn 'god (v! 0 0))))))

;;------------------------------------------------------------
